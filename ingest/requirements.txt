# Core pipeline (bot.py, parser.py, scorer.py, generate_data_js.py, review.py)
# uses ONLY the Python 3 standard library (urllib, json, re, argparse).
# Nothing to install for ingestion.

# Required ONLY by synthesize.py ("the brain") for Claude-powered theme digests.
# The core pipeline still runs with zero dependencies; install this just to run
# `python3 synthesize.py`:
anthropic>=0.40
